# Rust 和 ArkTS 建控件谁快

*[English version](CONCLUSION.md)*

在 SUP-AL90（HarmonyOS 6.1）上实测，release 构建。想复现的话，在 app 里打开
**Performance** 页，或者直接看 `hilog`。方法都写在
[`crates/splash-oh/src/bench.rs`](crates/splash-oh/src/bench.rs) 里。

## 结果

Rust 走 ArkUI NDK 比 ArkTS 快 2.6 到 3.2 倍，不是之前说的 45 倍。之前那个数是错的：
只实测了 Rust 这边，ArkTS 那边是拿另一个项目的常数推算出来的。

```
A  Rust → ArkUI NDK    ~17 µs/节点
B  ArkTS → typeNode    ~52 µs/节点
```

两边建的是同一个原生节点。`typeNode.createNode(ctx,'Text')` 和 NDK 的
`createNode(ARKUI_NODE_TEXT)` 最后都走到 libace 里同一个 `TextPattern`，属性也一样是
四个。2000 个节点，每次跑 10 轮，一共测了 6 次。

这个"同一个节点"很关键：后面的布局、测量、绘制两边是同一份原生代码，可以约掉，
所以只有"建"这一步有差别，这次测的也就是这一步。

## 差在哪

| 每次调用 | Rust | ArkTS | 倍数 |
|---|---|---|---|
| `createNode` | 14.0 – 17.3 µs | 34.6 – 41.2 µs | 2.1 – 2.6× |
| `setAttribute` | **0.025 µs** | **1.15 µs** | **46×** |
| 一次 JS → native napi 调用 | — | **0.058 µs** | — |
| 一次空 JS 循环迭代 | — | 0.017 µs | — |

桥本身只有 58 纳秒，不是瓶颈。ArkTS 设一个属性要 1.15 µs，其中过桥只占 5%，
剩下 95% 都在 JS 这边：`attribute` 那个 modifier 对象、参数装箱、分发、校验。
Rust 那 25 纳秒就是一次函数指针调用加填一个小结构体。

真正费时间的是 **JS 对象**。`typeNode.createNode` 除了建原生节点，还要建包装对象、
注册 finalizer、维护跨语言引用，之后 GC 还得把这些收掉。波动也是这么来的：
ArkTS 一轮一轮之间在 43 到 85 µs 之间跳，而 Rust 拆开单独测的那几个数，
三位有效数字都不带变的。

下面这两行在几次运行之间几乎不动（属性是 1.146 / 1.149 / 1.152 / 1.154 / 1.158 /
1.162 µs，过桥是 0.058 / 0.059 µs），所以结论靠它们撑着，上面那些抖得厉害的整节点
数字不用背这个锅。

## 不是预热

按顺序排的十轮：

```
A  Rust    13.4 12.9 15.5 17.9 16.0 15.9 20.9 24.6 20.5 16.7
B  ArkTS   47.1 81.5 85.3 47.3 47.5 55.5 44.5 50.4 50.0 81.5
```

预热的话曲线应该一路往下。B 没往下，就是来回跳，看不出趋势。

但要是不给事件循环留空隙、连着跑完，B 就变成一路往上：**37.9 → 87.4**。
这就是堆里每轮攒 2000 个包装对象的样子。中间给事件循环留出空隙，曲线就从"爬坡"
变成"抖动"。是 GC，不是 JIT。

（A 也在慢慢往上飘，但它这条路上根本没有分配器，那是设备在发热。两边是在同一个
tick 里交替跑的，就是为了让这种影响平摊到双方头上。）

## 那为什么还要用 NDK

其实不是图快。是因为 napi 的延迟跟 JS 线程忙不忙直接相关，而建控件树本身就会把
JS 线程占满。

octos-one 那边测出来一次往返 1.05 ms，其中 730 µs 纯粹是在排队等。同样一次跨越，
线程空着的时候只要 31 µs 左右。两个数都没测错，是我把线程争用当成桥的开销了。

所以：建控件快 3 倍是一方面，更要紧的是它不排队 —— UI 线程越忙，这个差距只会越大。
这个说法比这个仓库一开始的说法弱得多，但它是数据能支撑的那个。

## 内存

同一种节点、同一个进程，RSS 从 `/proc/self/status` 读，每个阶段前后各采一次。
8000 个控件，每个事件循环 tick 建 1000 个，然后全部释放，再在四秒里采十次样，
期间不做任何别的分配。

### 一个控件多少字节

| | 每个节点的边际开销 |
|---|---|
| **Rust → ArkUI NDK** | **~46 KB** |
| **ArkTS → typeNode** | **~50 KB** |

取 1000→8000 这一段算的，把第一批的初始化开销排除在外：
Rust 是 `(496 928 − 175 032) / 7000 = 46.0 KB`，ArkTS 是
`(534 584 − 187 704) / 7000 = 49.6 KB`。

**ArkTS 多花大约 8%，每个控件约 3.6 KB。** 这就是那个 JS 包装对象、它的
finalizer 和跨语言引用跟踪的代价。确实存在，但比它在**时间**上的 2.6–3.2 倍
小得多。包装对象在字节上便宜，在 CPU 上贵。

### 真正要紧的数字

**一个 `Text` 控件占用约 46 KB 常驻内存**，这是原生 ArkUI 节点本身的开销，
两条路都得付。它把这里其他所有开销都盖过去了。

```
    2 000 个控件  ≈   92 MB
    8 000 个控件  ≈  350 MB
   30 000 个控件  ≈  1.1 GB
```

到 30 000 的时候 app 直接死了。倒不是 OOM，是 `THREAD_BLOCK_6S`——一口气分配
这么多同时也把 UI 线程卡住了。但不管怎么说，一棵控件树占 1.1 GB，在手机上都不是
能干的事。

这个结果与其说是支持前面的时间结论，不如说是重新定位了它。建 2000 个控件，
Rust 约 40 ms，ArkTS 约 105 ms，差 65 ms，而且只差这一次。但两边都要占 92 MB，
而且是一直占着。**控件树先撞到的是内存墙，不是构建时间。** 如果选 NDK 路线的理由
是"这样能多放点控件"，那内存这边两种语言都说不行。

### 释放

| | 释放瞬间 | 空闲 4 秒后 |
|---|---|---|
| **Rust** | 立刻还回 42% | 不再变化 |
| **ArkTS** | **一点没还**，RSS 反而涨了 21 MB | 不再变化 |

Rust 是 `drop`，ArkTS 是把引用一丢然后等。在整个观测窗口里，ArkTS 那 350 MB
一点没还回来，释放的瞬间 RSS 还往上跳了一点。

有一个必须说清楚的地方：Rust 剩下那 58% 后来是还回来了 —— 但要等到 ArkTS 阶段
开始分配的时候。它在 350 MB 上平了整整四秒，然后一有人要内存就掉到了约 163 MB。
也就是说分配器会一直攥着页不放，直到有人来要。所以**"内存还回来了"很难证明，
"内存没还回来"很容易证明** —— 这是个不对称，把任何一列直接读成"泄漏"之前
得先想到这点。ArkTS 那边没有得到同样的"后来"机会，因为它之后没有别的分配了。

### 这次没测的

强制 GC 之后 ArkTS 那边会不会回收（release 构建里没有
`ArkTools.forceFullGC`）；跑上几个小时而不是几秒会怎样；以及**挂到树上**的控件 ——
这里所有节点都是游离的，所以真正显示出来的树还要额外付的布局、文字排版、
render node 那些内存，一点都没算进去。屏幕上真实的单控件开销，两条路都比 46 KB 高。

## 拿真实 app 试：把 makepad_wechat 完整写两遍

前面那个 microbenchmark 建的是 2000 个一模一样的 `Text` 节点，那不是 app。所以把
[project-robius/makepad_wechat](https://github.com/project-robius/makepad_wechat)
这个 WeChat demo 完整**重写成了两个能跑的 app**：

- **Rust → ArkUI NDK** —— [`crates/splash-oh/src/wechat/`](crates/splash-oh/src/wechat/)
- **ArkTS → typeNode** —— [`WeChatArkTs.ets`](deveco/entry/src/main/ets/pages/WeChatArkTs.ets)

两个都是真的能用，不是测试用的假页面：用的是原项目**自己的图片资源** ——
六张用户头像、WeChat Team 图标、所有菜单图标和四个 tab 的 SVG，都打包在
`rawfile/wechat/` 里 —— 加上同样的十二个会话、原项目里的人名和日文
消息内容、同样的四个 tab、同样的 `StackNavigation` 行为（点会话进消息页、点
Moments 或 My Profile 进对应页、返回退栈）。点一行是真的会跳转。数据只有一份，
在 Rust 这边，通过 napi 传给 ArkTS，所以两边不可能渲染出不同的内容。

两个实现挂在**同一个** `NodeContent` 上，标题栏那个按钮可以在 app 运行中切换
是谁在驱动界面。点下去屏幕应该看不出变化 —— 这就是重点：让这个对比能用眼睛
检查，而不是只能看秒表。

![rust](wechat-rust.jpeg)
![chat](wechat-chat.jpeg)

### 结果

跑完六个页面算一轮。每种条件 4 轮取中位数，前面另有 4 轮热身直接丢掉。

| 页面 | 空闲 | 有负载 |
|---|---|---|
| Chats（12 行） | 7.5 → 19.2 ms，2.55× | 8.5 → 21.7 ms，2.57× |
| Contacts（39 行） | 8.8 → 23.8 ms，2.72× | 8.6 → 22.4 ms，2.61× |
| Discover | 3.4 → 13.2 ms，3.83× | 3.9 → 8.8 ms，2.29× |
| Me | 6.5 → 15.7 ms，2.43× | 4.4 → 12.9 ms，2.91× |
| Chat（32 条消息） | 13.4 → 23.8 ms，1.78× | 8.2 → 20.6 ms，2.53× |
| Moments | 5.8 → 12.8 ms，2.19× | 4.7 → 11.4 ms，2.42× |
| **整轮** | **54.1 → 123.4 ms，2.28×** | **44.2 → 125.2 ms，2.83×** |

**真实 app 上约 2.3–2.8 倍**，跟合成 benchmark 的 2.6–3.2 倍对得上。由
`Row`/`Column`/`Text`/`Image` 混合而成的真实树，表现和 microbenchmark 预测的一致。

### 一个值得记下的资源坑

原项目那四个 tab 的 SVG，`viewBox` 写的是 `"0 0 24 24"`，但路径数据一直画到
约 485。makepad 不管 viewBox，直接按路径算边界，所以在它那儿显示正常。ArkUI
是认 viewBox 的，于是就裁成了左上角一小块空白，整个 tab 栏是空的。修正过的
viewBox 在 `rawfile/wechat/` 里；PNG 不用动。

要移植 makepad 应用的话这点值得知道：那些"本来就能用"的资源，可能只是在依赖
makepad 比较宽容。

### 负载并没有把差距拉大，而我原本以为会

做这件事的起因是一个很具体的预测：超级 app 的 JS 线程从来不空闲，而 napi 往返
恰恰在这种条件下劣化得厉害（octos-one 里空闲 31 µs、繁忙 1051 µs），所以差距
应该会从 3 倍拉到 30 倍那个量级。

**并没有。** 在合成的超级 app 负载下（每 8 ms 一批消息的 JSON 解析和序列化、
数组翻搅、promise 解析），倍数只从 2.39× 动到 2.56×，在多次运行的噪声范围之内。

老实说原因是测试装置的局限，不是这个想法被证伪：**在这里两条路都跑在 JS 线程上。**
benchmark 是从 ArkTS 通过 napi 调进 Rust 的，所以 Rust 和 ArkTS 一样排在同一个
负载后面。app 本身不是这样跑的 —— 点一行会话时，点击是从 ArkUI 事件线程过来的，
Rust 在那个线程上重建，完全不碰 JS 循环。要测那个，得让构建从事件线程触发，
而这套 benchmark 没有这么做。

所以关于负载的预测仍然没被验证。现在被验证的是：**那个约 2.5 倍在带真实导航的
真实 app 上是成立的**，而这一点之前只是推测。

### 内存

之前那次测砸了，是因为两个实现共用一个进程。这次重新做：**一次进程只测一个实现**，
因为污染是进程内的。

**稳定状态** —— app 挂上、停在会话列表上，全新进程：

| | RSS |
|---|---|
| Rust → NDK | 157 – 159 MB |
| ArkTS → typeNode | 155 – 163 MB |

**没有有意义的差别。** 两边都在 157–160 MB 左右，而且这里面几乎全是 ArkUI 运行时
的底噪，不是 app 本身。用哪种语言建控件，不改变这个 app 跑起来占多少内存。

**边际** —— 然后再叠 12 份完整的 app（每份 6 个页面，共 72 个页面），看 RSS 怎么涨：

```
份数    Rust        ArkTS
  4     235 MB      253 MB
  8     321 MB      371 MB
 12     406 MB      451 MB
```

| | 每份 app | 每个页面 |
|---|---|---|
| Rust → NDK | **21.5 MB** | 3.6 MB |
| ArkTS → typeNode | **24.8 MB** | 4.1 MB |

**ArkTS 多约 15%**，就是每个节点那个 JS 包装对象和它的 finalizer —— 跟纯 `Text`
节点上看到的 8% 是一回事，这里大一些是因为这些树是 `Row`/`Column`/`Image` 混合
而且是挂上父子关系的。

所以内存上两边差得不多，而对手机来说真正要紧的数字是：两边都不便宜 —— 这个 app
的一个页面约 4 MB，而在第一个控件出现之前，框架底噪就已经是约 157 MB 了。

### 更早那次内存测试是怎么砸的

在两条路上各堆整页、看 RSS，结果是这样：

| | 先跑的 | 后跑的 |
|---|---|---|
| Rust 先跑那次 | Rust **每页 +3613 kB** | ArkTS 每页 +691 kB |
| ArkTS 先跑那次 | ArkTS **每页 +1861 kB** | Rust **负数** |

后跑的那一方是在把前一方释放、但分配器攥着没还给内核的页重新填满，所以看上去
几乎不要钱 —— 有一次甚至是在建 30 个页面的过程中 RSS 反而在往下掉。这些数字
跟踪的是构建顺序，不是内存。**这个方法没法在同一个进程里同时测两条路的单控件内存**，
而把顺序翻过来跑是发现这一点的唯一办法；单看任何一次运行，结果都像模像样。

真正站得住的单节点内存数字还是前面那个专门的单路径 ramp：
**每个控件约 46 KB，ArkTS 多约 8%**。

### 超级 app 该从这里拿走什么

构建大约快 2.5 倍，在带真实导航的真实页面上验证过。放到单个页面上就是 19 ms
变成 8 ms —— 你盯着看能看出来，不盯着看不出来。它变成真问题是在构建持续发生的
时候：一次滑动重建一个长列表，这个代价是每帧都要付的，而 120 Hz 的一帧里塞得下
8 ms，塞不下 19 ms。

更强的那个论点 —— Rust 不用排在繁忙的 JS 线程后面 —— 仍然没测到，
而且这套装置在结构上就测不了。

## 局限

- **测量 C（napi 往返）在现在这版跑不起来。** 把整套测试改成由 ArkTS 一个 tick 跑
  一轮之后，JS 那边就不再确认 worker 发过去的消息了，原因还没查出来。README 里标了
  那几个数是旧版测的。worker 现在会超时报错，不会像以前那样一直挂着。
- **28 个页面只看了 6 个**，剩下 22 个没逐个验证。用的是同一套 DSL 辅助函数和同一个
  遍历器，但确实没看。
- **没在负载下测过。** 而"线程争用"这个说法到底成不成立，恰恰得在负载下才能验证，
  空闲状态的 microbenchmark 说明不了问题。
- **内存只测了游离的控件。** 一个都没挂到树上，所以布局和 render node 那部分内存
  不在 46 KB 里面。
- ArkTS 的**声明式**那套完全没测。`typeNode` 是它的命令式口子，选它就是因为只有它
  能跟 NDK 做等价对比。

## 三个测量上的坑

每一个都给出了看着挺像回事、其实是错的数字：

1. **拿常数推算。** ArkTS 那边压根没测过，是 `1051 µs × 控件数` 算出来的 ——
   那个 1051 还是从另一个 app 里测的往返时间，而且默认了"每个控件过一次桥"这个从没
   验证过的前提。
2. **相减的结果比噪声还小。** 用（完整试验 − 只建节点）去算每个属性的开销，
   Rust 那边算出来是**负数**。信号本身不到 1 µs，而两次 2000 节点试验之差的噪声底
   是它的好几倍。现在属性是单独直接测的。
3. **事件循环被卡住。** 在 `mount()` 里跑 benchmark，把 JS 线程占得太久，定时器队列
   直接不转了，结果整个 ArkTS 那半套测试静默地根本没跑 —— 不报错、不输出，就是没结果。
   现在每一轮都由 ArkTS 驱动，一个 tick 跑一轮。

还有个小的：ArkTS 的 `@Component` struct 里声明的 `static` 成员，运行时读出来是
`undefined`，于是状态机一路穿下去也不报错。改成模块级 `const` 就好了。
