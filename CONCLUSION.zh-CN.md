# Rust 和 ArkTS 建控件谁快

*[English version](CONCLUSION.md)*

下面所有数字都是在 SUP-AL90（HarmonyOS 6.1）上实测的，release 构建。想复现：
`./build.sh` 然后看 `hilog`；内存要用 `./memrun.sh <app> <path>`，一次进程只测一条路。

---

## 结论

**用 Rust 通过 ArkUI NDK 建控件树，比用 ArkTS 通过 `typeNode` 建同一棵树快约
2.5–3 倍，内存少约 9–18%。**

这比这个仓库一开始说的要小得多，但它是数据能撑住的那个。中间撤回过两个数字，
两个都在下面写清楚了，而不是悄悄改掉。

### 依据

四个 makepad 参考应用，每个都实现了两遍 —— 一遍 Rust 走 NDK，一遍 ArkTS 走
`typeNode` —— 然后在真机上一页一页地跑：

| app | 形态 | 每轮节点数 | 空闲 | 有负载 |
|---|---|---|---|---|
| [WeChat](https://github.com/project-robius/makepad_wechat) | 文字为主的列表 | 592 | 2.83× / 2.56× | 2.53× / 1.89× |
| [Taobao](https://github.com/project-robius/makepad_taobao) | 双列商品网格 | 502 | 2.47× / 2.75× | 1.93× / 2.39× |
| [TikTok](https://github.com/project-robius/makepad_tiktok) | 全屏媒体 | 446 | 2.32× / 2.41× | 2.70× / 2.78× |
| [Wonderous](https://github.com/project-robius/makepad_wonderous) | 图文长页和照片 | 234 | 3.05× / 2.49× | 2.34× / 2.59× |

每个格子跑了两次。四种差别很大的控件构成 —— 文字列表、图片网格、视频界面、
图文长页 —— 结果全部落在 **2.3–3.1×**。

这个区间才是这里最有用的东西。它说明**这个差距的行为像是每个节点一个固定值**，
跟节点是什么关系不大。只做一个 app 是得不出这个结论的。

![taobao](app-taobao.jpeg)
![tiktok](app-tiktok.jpeg)
![wonderous](app-wonderous.jpeg)

### 这个对比为什么可信

`typeNode.createNode(ctx, 'Text')` 和 NDK 的 `createNode(ARKUI_NODE_TEXT)`
最后都落到 libace 里同一个 C++ `TextPattern`。不是"等价的节点"，就是同一个节点。
所以之后的测量、布局、绘制都是同一份原生代码，会相互抵消；只有"建"这一步有差别。

两个实现的内容都来自同一处（在 Rust 这边，通过 napi 传给 ArkTS），用的也是原项目
自己的素材。**节点数在运行时逐页逐轮校验** —— 这里报告的所有运行都没有出现
MISMATCH。"另一边快是因为它偷偷少建了"是这种对比最容易出错的地方，所以是校验，
不是假设。

两边还挂在同一个 `NodeContent` 上，标题栏那个按钮可以在运行中切换是谁在驱动界面，
切过去屏幕看不出变化。

---

## 差在哪

每一行都是直接测的，不是拿两次有噪声的试验相减推出来的：

| 每次调用 | Rust | ArkTS | 倍数 |
|---|---|---|---|
| `createNode` | 14.0 – 17.3 µs | 34.6 – 41.2 µs | 2.1 – 2.6× |
| `setAttribute` | **0.025 µs** | **1.15 µs** | **46×** |
| 一次 JS → native napi 调用 | — | **0.058 µs** | — |
| 一次空 JS 循环迭代 | — | 0.017 µs | — |

### 不是桥的问题

一次 JS → native 的 napi 调用要 **58 纳秒**。ArkTS 设一个属性要 1.15 µs，
其中过桥只占约 5%，剩下 95% 都在 JS 这边：`attribute` 那个 modifier 对象、
参数装箱、分发、校验。Rust 那 25 纳秒就是一次函数指针调用加填一个小结构体。

### 也不是预热

按顺序排的十轮：

```
A  Rust    12.9 12.3 14.1 16.4 20.2 20.4 20.9 25.4 16.5 21.1
B  ArkTS   53.2 78.0 73.7 50.5 52.7 57.3 43.9 44.9 49.2 83.2
```

预热应该是一路往下。B 没往下，就是来回跳，看不出趋势。要是**不**给事件循环留空隙、
连着跑完，它反而一路往上爬 —— **37.9 → 87.4** —— 那是堆里每轮攒 2000 个包装对象
的样子。中间留出空隙，曲线就从"爬坡"变成"抖动"。这是 GC，不是 JIT。

### 是对象的产生与回收

`typeNode.createNode` 不只是建一个原生节点：它还要建一个 JS 包装对象、注册
finalizer、维护跨语言引用跟踪，之后 GC 还得把这些全收掉。这就是创建那 21 µs 的
差距，也是"拆开算"（37 + 5×1.15 ≈ 43 µs）和"实测整节点"（约 52 µs）之间那段差的
来源。

---

## 内存

每个 app、每种实现都用一个全新进程测 —— 污染是进程内的，这一点弄错就产生了
下面两个撤回中的一个。

**稳定状态** —— app 挂上、停住之后：

四个 app、两条路，全都是 **约 165–170 MB**。这里面几乎全是 ArkUI 运行时的底噪。
**用哪种语言建控件，不改变这个 app 跑起来占多少内存。**

**边际开销** —— 往上叠 12 份完整 app 的斜率：

| app | Rust | ArkTS | ArkTS 多用 |
|---|---|---|---|
| WeChat | 21.5 MB | 24.8 MB | +15% |
| Taobao | 19.5 MB | 21.2 MB | +9% |
| TikTok | 16.8 MB | 19.6 MB | +17% |
| Wonderous | 7.3 MB | 8.6 MB | +18% |

**+9% 到 +18%** —— 就是每个节点那个 JS 包装对象和它的 finalizer。确实存在，
但比同一个包装对象在**时间**上的 2.5–3 倍小得多。在字节上便宜，在 CPU 上贵。

### 比倍数更要紧的数字

一个 `Text` 控件占用 **约 46 KB 常驻内存**（上面那些混合树里是 29–39 KB，
因为含有更便宜的容器）。这是原生 ArkUI 节点本身的开销，两条路都得付。

```
  2 000 个控件 ≈  92 MB
  8 000 个控件 ≈ 350 MB
 30 000 个控件 ≈ 1.1 GB   ← app 死在这里
```

建 2000 个控件，两条路差约 65 ms，**就差这一次**。但两边都要占 92 MB，
而且**一直占着**。控件树先撞到的是内存墙，不是构建时间 —— 所以"这样能多放点控件"
不是 NDK 路线能换来的东西。

### 回收

释放的瞬间，Rust 立刻还回 42%，剩下的要等到后来有别人开始分配时才还 ——
在那之前它在原地平了整整四秒。ArkTS 在同样的窗口里一点没还。

把这一列直接读成"泄漏"之前，得先想到这个不对称：分配器会一直攥着释放的页不放，
直到有人来要。所以**"内存还回来了"很难证明，"内存没还回来"很容易证明**。
ArkTS 那边没有得到同样的"后来"机会，因为它之后没有别的分配了。

---

## 两次撤回

### 45× → 2.5×

最初那个说法，是拿**实测**的 Rust 开销去比**推算**的 ArkTS 开销：
`1051 µs × 控件数`，而那个 1051 µs 是在另一个 app 里测的一次往返，
前提是"每个控件过一次桥"—— 这个前提从没验证过。

好好测下来，ArkTS 建同一个节点约 60 µs，Rust 约 24 µs。

**而且那个 1051 µs 本身就标错了。** 同样一次跨越，在这里空闲线程上是约 31 µs，
JS → native 方向是 58 纳秒。octos-one 里那次是 JS 线程正忙，1051 里有 730 µs
是那个消息在队列里干等。所以：

> **napi 不慢。排在繁忙的 JS 线程后面才慢。**

这把整个论证重新定位了。桥的延迟是负载的函数，而建控件树本身就是负载。

### TikTok +50% → +17%

TikTok 的内存测出来是 +50%，另外三个是 +9–18%，本文档早先的版本把它当成
"无法解释的异常值"报了出去。

那是测试装置的 bug。计时那一组用的 `build_route` 把 TikTok 的 `feed` 路由映射到
`build_feed()`（五个 reel 全建）；而**内存**那一组用的 `set_route` 里根本没有
`feed` 分支，直接落到了单个 reel。Rust 只拿着一个 reel，ArkTS 的对照拿着五个，
那个差距是工作量的差，不是开销的差。

值得记下它是怎么浮出来的：不是 benchmark 发现的，它给出的数字干干净净；
是因为追问"为什么这一个 app 跟另外三个不一样"。**没解释清楚的结果，在被证明之前
都该当成缺陷。**

---

## 实际意味着什么

**对一个普通页面，什么都不意味着。** 8 ms 的 2.5 倍是 20 ms —— 你盯着找能看出来，
不盯着看不出来。内存跟那 157 MB 的框架底噪一比更是零头。

**它变成真问题是在构建持续发生的时候。** 一次滑动重建一个长列表，这个差价每帧都要付，
而 120 Hz 的一帧里塞得下 8 ms，塞不下 20 ms。一个超级 app 整个会话都在建和拆视图树，
这个代价是反复付的，不是只付一次。

**最强的那个论点仍然没测到。** Rust 不用排在繁忙的 JS 线程后面 —— 但这套装置证明不了，
原因在下面。

---

## 局限

- **"有负载"那一列什么都没证明。** benchmark 是**从 ArkTS 通过 napi**调进 Rust 的，
  所以两条路排在同一个负载发生器后面。真实架构不是这样 —— 点击是从 ArkUI 事件线程
  过来的，Rust 在那个线程上重建，完全不碰 JS 循环 —— 但要测那个，得让构建从事件线程
  触发，而这套装置没有。所以"有负载"下的约 3 倍跟空闲是同一个测量，不是第二个。
- **测量 C（napi 往返）在当前构建里跑不起来。** 那几个数来自更早的构建。worker 现在
  会超时并报出来，不会一直挂着。
- **没有视频。** NDK 里没有 `ARKUI_NODE_VIDEO`（跟 `ARKUI_NODE_WEB` 一样的空缺），
  所以 TikTok 显示的是从它自己的 mp4 里抽的封面帧。两边替换方式一样；两边的数字都不含解码。
- **没有视差。** Wonderous 那个由 shader 驱动的头部没有移植。假做一个只会测到那个假的。
- **单节点内存是在游离的树上测的**；真正挂上去的控件还会加上布局和 render node 的内存，
  那 46 KB 里不含这部分。
- **只有一台设备。** SUP-AL90，HarmonyOS 6.1，一颗 SoC。

---

## 最可能被别人用上的部分

六个测量缺陷，每一个都产出了看着自信、合理、但错误的数字：

1. **拿推算的常数**顶替测量（那个 45 倍）。
2. **相减的结果比噪声底还小** —— 用（完整试验 − 只建节点）算每个属性的开销，
   Rust 那边算出来是**负数**。
3. **事件循环被卡住** —— 在 `mount()` 里跑 benchmark，把 JS 线程占太久，
   定时器队列不转了，半套测试静默地根本没跑。不报错、不输出，就是没结果。
4. **在采样窗口里放了一个诱发 GC 的循环**，把它自己想读的 RSS 撑大了。
5. **两组的采样次数不对称** —— 一边 2 次，一边 5 次。
6. **两组建的根本不是同一个东西**（TikTok 的 `feed` 路由）。

另外还有三个必须靠设计消掉的顺序效应：条件按"先空闲后负载"排，结果持续的 JIT 热身
让 ArkTS 在负载下看起来**更快**；两条路在同一个 tick 里建，先建的那个要替上一个 tick
的清理付约 10 ms，于是单页倍数纯粹因为建的顺序在 1.06× 到 8.60× 之间乱跳；
内存那一组里，后测的那一方总在把先测的那方释放、分配器却攥着没还的页重新填满，
于是看上去几乎不要钱，有一种排法下甚至是负数。

**每一个都是被完备性检查抓到的，不是被 benchmark 抓到的。** 一个不可能存在的负数。
一行缺失的日志。一组采样比另一组少。一个 app 跟另外三个对不上。这些情况下
benchmark 给出的数字全都干干净净。

**把完备性检查建起来。数字看上去都会没问题。**
