[package]
name = "splash-oh-plugin-demo"
version = "0.1.0"
edition = "2021"
description = "A Splash-OH plugin, existing to prove a tool can live outside the bridge"

[dependencies]
splash-oh-core = { path = "../splash-oh-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1.0.151"
