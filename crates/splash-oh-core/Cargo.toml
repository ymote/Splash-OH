[package]
name = "splash-oh-core"
version = "0.1.0"
edition = "2021"
description = "The tool registry a Splash-OH plugin registers into"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1.0.151"
