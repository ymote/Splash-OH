[package]
name = "splash-oh-webview"
version = "0.1.0"
edition = "2021"
description = "A Tauri-style JS to Rust bridge for OpenHarmony web surfaces, over native ArkUI"

# Named `splash_oh` deliberately: the artifact stays libsplash_oh.so, which is
# what ArkTS imports. The package name records the split; the .so name is an
# interface and does not need to churn for it.
[lib]
name = "splash_oh"
crate-type = ["cdylib"]

[dependencies]
splash-oh-native = { path = "../splash-oh-native" }
splash-oh-core = { path = "../splash-oh-core" }
# The cdylib is what links a plugin in. A plugin crate cannot register itself
# into a library it is not part of, which is why this dependency lives here and
# not in splash-oh-core -- see the module docs there.
splash-oh-plugin-demo = { path = "../splash-oh-plugin-demo" }
# The Splash VM, via ymote/Splash rather than straight from makepad.
splash-core = { git = "https://github.com/ymote/Splash.git", branch = "main" }
napi-derive-ohos = "0.0.9"
napi-ohos = "0.1.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1.0.151"
